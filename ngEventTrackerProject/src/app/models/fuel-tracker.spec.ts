import { FuelTracker } from './fuel-tracker';

// import { FuelTracker } from './fuel-tracker';

describe('FuelTracker', () => {
  it('should create an instance', () => {
    expect(new FuelTracker()).toBeTruthy();
  });
});
